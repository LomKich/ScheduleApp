<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Schedule" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0D0D0D</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#0D0D0D</item>
    </style>

    <!-- Прозрачная тема для кружков — поверх чата без чёрного экрана -->
    <style name="Theme.CircleRecord" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
