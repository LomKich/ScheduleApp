package com.schedule.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Фон / поверхности ─────────────────────────────────────────
val BgPrimary       = Color(0xFF0D0D0D)   // Основной фон — почти чёрный
val BgSurface       = Color(0xFF161616)   // Поверхность (toolbar, nav)
val BgCard          = Color(0xFF1E1E1E)   // Карточки, list items
val BgCardElevated  = Color(0xFF252525)   // Карточки с elevation
val BgDialog        = Color(0xFF2A2A2A)   // Диалоги

// ── Акцент: индиго-фиолетовый (Echo Nightly) ──────────────────
val AccentPrimary          = Color(0xFF6B6BF5)
val AccentPrimaryDim       = Color(0xFF4A4ACF)
val AccentPrimaryContainer = Color(0xFF2A2A5A)
val AccentOnPrimary        = Color(0xFFFFFFFF)

// ── Вторичный акцент: сиреневый ───────────────────────────────
val AccentSecondary          = Color(0xFFB06BF5)
val AccentSecondaryContainer = Color(0xFF3A2A5A)

// ── Пинк (shuffle / активные иконки) ──────────────────────────
val AccentPink = Color(0xFFFF6BAE)

// ── Текст ─────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF9E9E9E)
val TextHint      = Color(0xFF616161)
val TextAccent    = Color(0xFF9090FF)

// ── Разделители / обводки ─────────────────────────────────────
val Divider = Color(0xFF2C2C2C)
val Outline = Color(0xFF3A3A3A)

// ── Ошибки ────────────────────────────────────────────────────
val ErrorColor          = Color(0xFFCF6679)
val ErrorContainerColor = Color(0xFF4A1020)

// ── Светлая тема (fallback) ───────────────────────────────────
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
